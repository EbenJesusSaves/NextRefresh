import { searchBoysProps, searchMeProps } from '@/types'
import React from 'react'

export const TestingForBoys = ({ king, }:searchBoysProps ) : number [] => {

  return [23]
}


const kwameDushBug =({array}:searchMeProps<string>)=>{

    array = ['dushBugs ']
}